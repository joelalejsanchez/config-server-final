server:
  port: 8081

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://registry-server:8761/eureka
